[project]
name = "chosestock"
version = "0.1.0"
description = "A-share trading assistant skills for Claude Code"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "akshare>=1.13.0",
    "pandas>=2.0.0",
]

[tool.uv]
package = false
